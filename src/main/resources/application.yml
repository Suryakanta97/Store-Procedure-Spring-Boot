spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/ORCLCDB.localdomain
    username: oracledba
    password: admin
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    database: oracle
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    serialization:
      indent-output: true